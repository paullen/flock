Random {
    `SELECT * FROM RandomSource WHERE First= @first, Third = @third`
    {
        First = first | toGuid "usr"
        Second = two | Nil 2
        Third = three | Decrypt "oldkey" | Encrypt "newkey"
    }
}
